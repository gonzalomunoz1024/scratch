# Current data size on the source PVC
oc exec opensearch-cluster-masters-0 -n opensearch -c opensearch -- du -sh /usr/share/opensearch/data 2>&1

# What's actually in there — sanity check there's real data and not just metadata
oc exec opensearch-cluster-masters-0 -n opensearch -c opensearch -- ls /usr/share/opensearch/data/nodes/0/indices/ 2>&1 | head -20

# Storage class details — confirms what backup PVC should request
oc get pvc data-opensearch-cluster-masters-0 -n opensearch -o jsonpath='{.spec.storageClassName}{"\n"}{.spec.resources.requests.storage}{"\n"}{.spec.accessModes}{"\n"}'