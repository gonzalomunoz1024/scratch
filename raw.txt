chmod +x 04-export-dashboards.sh 05-import-dashboards.sh

docker compose up -d
# This will start the new dashboards container alongside the running
# opensearch one. First boot of dashboards takes ~1-2 minutes while it
# initializes the .kibana_1 index — but yours already exists from your
# original data, so it'll be fast.

# Wait until it reports healthy:
docker compose ps
# dashboards should show "healthy" — if "starting", give it a minute