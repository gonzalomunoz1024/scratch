# What is the StatefulSet currently saying?
oc describe statefulset opensearch-cluster-masters -n opensearch | tail -20

# Recent events — should show whether SCC errors are gone or still happening
oc get events -n opensearch --sort-by='.lastTimestamp' | tail -15